Fork the current Claude Code session and switch to the fork in a new tmux session.

Arguments: $ARGUMENTS (the name for the fork — required)

Run: `bash ~/.claude/scripts/fork-this.sh $ARGUMENTS`

Tell the user: "Forked to **$ARGUMENTS** — switched to new tmux session."
