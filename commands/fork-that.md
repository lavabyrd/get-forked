Fork the current Claude Code session into a background tmux session. You remain in the current session.

Arguments: $ARGUMENTS (the name for the fork — required)

Run: `bash ~/.claude/scripts/fork-that.sh $ARGUMENTS`

Tell the user: "Forked to **$ARGUMENTS** — running detached. You remain here."
